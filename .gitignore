# ── Büyük veri dosyaları (GitHub 100MB limit) ──────────────────────────
# Bunlar Google Drive / harici disk ile ayrı transfer edilir.
data/chunks
data/chunks.jsonl
data/embeddings/
data/vector_db/

# Ham MinerU çıktıları (ZIP arşivleri, ~20GB)
*.zip
ER MinerU Dosyaları/
_pptx_work/

# ── Python ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# ── Model ağırlıkları (büyükse) ────────────────────────────────────────
# gat_weights.pt küçük (8KB) → dahil. Büyük checkpoint olursa buraya ekle.
*.ckpt
*.safetensors

# ── IDE / OS ───────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# ── Claude / oturum ────────────────────────────────────────────────────
.claude/

# ── HF cache (varsa repo içinde) ───────────────────────────────────────
.cache/
*.lock

# Loglar
*.log
